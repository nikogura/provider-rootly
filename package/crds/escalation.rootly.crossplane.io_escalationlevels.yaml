---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: escalationlevels.escalation.rootly.crossplane.io
spec:
  group: escalation.rootly.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - rootly
    kind: EscalationLevel
    listKind: EscalationLevelList
    plural: escalationlevels
    singular: escalationlevel
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EscalationLevel is the Schema for the EscalationLevels API. <no
          value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EscalationLevelSpec defines the desired state of EscalationLevel
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  delay:
                    description: Delay before notifying targets in the next Escalation
                      Level.
                    type: number
                  escalationPolicyId:
                    description: The ID of the escalation policy
                    type: string
                  escalationPolicyIdRef:
                    description: Reference to a EscalationPolicy in escalation to
                      populate escalationPolicyId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  escalationPolicyIdSelector:
                    description: Selector for a EscalationPolicy in escalation to
                      populate escalationPolicyId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  escalationPolicyPathId:
                    description: The ID of the dynamic escalation policy path the
                      level will belong to. If nothing is specified it will add the
                      level to your default path.
                    type: string
                  notificationTargetParams:
                    description: Escalation level's notification targets
                    items:
                      properties:
                        id:
                          description: The ID of notification target
                          type: string
                        teamMembers:
                          description: For targets with type=team, controls whether
                            to notify admins, all team members, or escalate to team
                            EP.. Value must be one of `all`, `admins`, `escalate`.
                          type: string
                        type:
                          description: The type of the notification target. Value
                            must be one of `team`, `user`, `schedule`, `slack_channel`,
                            `microsoft_teams_channel`, `service`.
                          type: string
                      type: object
                    type: array
                  pagingStrategyConfigurationPageUsersCount:
                    description: Number of users to page at a time (cycle-based round
                      robin).
                    type: number
                  pagingStrategyConfigurationRepeats:
                    description: Number of times to rotate through the roster (cycle-based
                      round robin).
                    type: number
                  pagingStrategyConfigurationRepeatsMode:
                    description: 'Controls how repeats are interpreted: ''users''
                      pages exactly N users, ''all'' pages everyone once.. Value must
                      be one of `users`, `all`.'
                    type: string
                  pagingStrategyConfigurationRotationScope:
                    description: 'Scope of rotation ordering: active rotation members
                      only, or entire schedule.. Value must be one of `active_rotation`,
                      `entire_schedule`.'
                    type: string
                  pagingStrategyConfigurationScheduleStrategy:
                    description: Value must be one of `on_call_only`, `everyone`.
                    type: string
                  pagingStrategyConfigurationStrategy:
                    description: Value must be one of `default`, `random`, `cycle`,
                      `alert`.
                    type: string
                  position:
                    description: Position of the escalation policy level
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  delay:
                    description: Delay before notifying targets in the next Escalation
                      Level.
                    type: number
                  escalationPolicyId:
                    description: The ID of the escalation policy
                    type: string
                  escalationPolicyIdRef:
                    description: Reference to a EscalationPolicy in escalation to
                      populate escalationPolicyId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  escalationPolicyIdSelector:
                    description: Selector for a EscalationPolicy in escalation to
                      populate escalationPolicyId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  escalationPolicyPathId:
                    description: The ID of the dynamic escalation policy path the
                      level will belong to. If nothing is specified it will add the
                      level to your default path.
                    type: string
                  notificationTargetParams:
                    description: Escalation level's notification targets
                    items:
                      properties:
                        id:
                          description: The ID of notification target
                          type: string
                        teamMembers:
                          description: For targets with type=team, controls whether
                            to notify admins, all team members, or escalate to team
                            EP.. Value must be one of `all`, `admins`, `escalate`.
                          type: string
                        type:
                          description: The type of the notification target. Value
                            must be one of `team`, `user`, `schedule`, `slack_channel`,
                            `microsoft_teams_channel`, `service`.
                          type: string
                      type: object
                    type: array
                  pagingStrategyConfigurationPageUsersCount:
                    description: Number of users to page at a time (cycle-based round
                      robin).
                    type: number
                  pagingStrategyConfigurationRepeats:
                    description: Number of times to rotate through the roster (cycle-based
                      round robin).
                    type: number
                  pagingStrategyConfigurationRepeatsMode:
                    description: 'Controls how repeats are interpreted: ''users''
                      pages exactly N users, ''all'' pages everyone once.. Value must
                      be one of `users`, `all`.'
                    type: string
                  pagingStrategyConfigurationRotationScope:
                    description: 'Scope of rotation ordering: active rotation members
                      only, or entire schedule.. Value must be one of `active_rotation`,
                      `entire_schedule`.'
                    type: string
                  pagingStrategyConfigurationScheduleStrategy:
                    description: Value must be one of `on_call_only`, `everyone`.
                    type: string
                  pagingStrategyConfigurationStrategy:
                    description: Value must be one of `default`, `random`, `cycle`,
                      `alert`.
                    type: string
                  position:
                    description: Position of the escalation policy level
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.notificationTargetParams is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.notificationTargetParams)
                || (has(self.initProvider) && has(self.initProvider.notificationTargetParams))'
            - message: spec.forProvider.position is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.position)
                || (has(self.initProvider) && has(self.initProvider.position))'
          status:
            description: EscalationLevelStatus defines the observed state of EscalationLevel.
            properties:
              atProvider:
                properties:
                  delay:
                    description: Delay before notifying targets in the next Escalation
                      Level.
                    type: number
                  escalationPolicyId:
                    description: The ID of the escalation policy
                    type: string
                  escalationPolicyPathId:
                    description: The ID of the dynamic escalation policy path the
                      level will belong to. If nothing is specified it will add the
                      level to your default path.
                    type: string
                  id:
                    type: string
                  notificationTargetParams:
                    description: Escalation level's notification targets
                    items:
                      properties:
                        id:
                          description: The ID of notification target
                          type: string
                        teamMembers:
                          description: For targets with type=team, controls whether
                            to notify admins, all team members, or escalate to team
                            EP.. Value must be one of `all`, `admins`, `escalate`.
                          type: string
                        type:
                          description: The type of the notification target. Value
                            must be one of `team`, `user`, `schedule`, `slack_channel`,
                            `microsoft_teams_channel`, `service`.
                          type: string
                      type: object
                    type: array
                  pagingStrategyConfigurationPageUsersCount:
                    description: Number of users to page at a time (cycle-based round
                      robin).
                    type: number
                  pagingStrategyConfigurationRepeats:
                    description: Number of times to rotate through the roster (cycle-based
                      round robin).
                    type: number
                  pagingStrategyConfigurationRepeatsMode:
                    description: 'Controls how repeats are interpreted: ''users''
                      pages exactly N users, ''all'' pages everyone once.. Value must
                      be one of `users`, `all`.'
                    type: string
                  pagingStrategyConfigurationRotationScope:
                    description: 'Scope of rotation ordering: active rotation members
                      only, or entire schedule.. Value must be one of `active_rotation`,
                      `entire_schedule`.'
                    type: string
                  pagingStrategyConfigurationScheduleStrategy:
                    description: Value must be one of `on_call_only`, `everyone`.
                    type: string
                  pagingStrategyConfigurationStrategy:
                    description: Value must be one of `default`, `random`, `cycle`,
                      `alert`.
                    type: string
                  position:
                    description: Position of the escalation policy level
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
