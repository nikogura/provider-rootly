---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: alertssources.alerts.rootly.crossplane.io
spec:
  group: alerts.rootly.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - rootly
    kind: AlertsSource
    listKind: AlertsSourceList
    plural: alertssources
    singular: alertssource
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AlertsSource is the Schema for the AlertsSources API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertsSourceSpec defines the desired state of AlertsSource
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  alertSourceFieldsAttributes:
                    description: 'List of alert fields to be added to the alert source.
                      Note: This attribute requires the alert field feature to be
                      enabled on your account. Contact Rootly customer support if
                      you need assistance with this feature.'
                    items:
                      properties:
                        alertFieldId:
                          description: The ID of the alert field
                          type: string
                        templateBody:
                          description: Liquid expression to extract a specific value
                            from the alert's payload for evaluation
                          type: string
                      type: object
                    type: array
                  alertSourceUrgencyRulesAttributes:
                    description: List of rules that define the conditions under which
                      the alert urgency will be set automatically based on the alert
                      payload
                    items:
                      properties:
                        alertUrgencyId:
                          description: The ID of the alert urgency
                          type: string
                        conditionableId:
                          description: The ID of the conditionable. If conditionable_type
                            is AlertField, this is the ID of the alert field.
                          type: string
                        conditionableType:
                          description: The type of the conditionable. Value must be
                            one of `AlertField or (empty string)`.
                          type: string
                        jsonPath:
                          description: JSON path expression to extract a specific
                            value from the alert's payload for evaluation
                          type: string
                        kind:
                          description: The kind of the conditionable. Value must be
                            one of `payload`, `alert_field`.
                          type: string
                        operator:
                          description: Comparison operator used to evaluate the extracted
                            value against the specified condition. Value must be one
                            of `is`, `is_not`, `contains`, `does_not_contain`.
                          type: string
                        value:
                          description: Value that the extracted payload data is compared
                            to using the specified operator to determine a match
                          type: string
                      type: object
                    type: array
                  alertTemplateAttributes:
                    description: Note that when alert fields are enabled at the team
                      level, alert template attributes cannot be provided. Use `alert_source_fields_attributes`
                      instead.
                    items:
                      properties:
                        description:
                          description: The alert description.
                          type: string
                        externalUrl:
                          description: The alert URL.
                          type: string
                        title:
                          description: The alert title.
                          type: string
                      type: object
                    type: array
                  alertUrgencyId:
                    description: ID for the default alert urgency assigned to this
                      alert source
                    type: string
                  deduplicateAlertsByKey:
                    description: Toggle alert deduplication using deduplication key.
                      If enabled, deduplication_key_kind and deduplication_key_path
                      are required.. Value must be one of true or false
                    type: boolean
                  deduplicationKeyKind:
                    description: Kind of deduplication key.. Value must be one of
                      `payload`.
                    type: string
                  deduplicationKeyPath:
                    description: Path to deduplication key. This is a JSON Path to
                      extract the deduplication key from the request body.
                    type: string
                  deduplicationKeyRegexp:
                    description: Regular expression to extract key from value found
                      at key path.
                    type: string
                  email:
                    description: The email generated for email alert sources
                    type: string
                  name:
                    description: The name of the alert source
                    type: string
                  ownerGroupIds:
                    description: List of team IDs that will own the alert source
                    items:
                      type: string
                    type: array
                  resolutionRuleAttributes:
                    description: Provide additional attributes for email alerts source
                    items:
                      properties:
                        conditionType:
                          description: The type of condition to evaluate to apply
                            auto resolution rule. Value must be one of `all`, `any`.
                          type: string
                        conditionsAttributes:
                          description: List of conditions to evaluate for auto resolution
                          items:
                            properties:
                              conditionableId:
                                description: The ID of the conditionable. If conditionable_type
                                  is AlertField, this is the ID of the alert field.
                                type: string
                              conditionableType:
                                description: The type of the conditionable. Value
                                  must be one of `AlertField or (empty string)`.
                                type: string
                              field:
                                description: JSON path expression to extract a specific
                                  value from the alert's payload for evaluation
                                type: string
                              kind:
                                description: The kind of the conditionable. Value
                                  must be one of `payload`, `alert_field`.
                                type: string
                              operator:
                                description: Comparison operator used to evaluate
                                  the extracted value against the specified condition.
                                  Value must be one of `is`, `is_not`, `contains`,
                                  `does_not_contain`, `starts_with`, `ends_with`.
                                type: string
                              value:
                                description: Value that the extracted payload data
                                  is compared to using the specified operator to determine
                                  a match
                                type: string
                            type: object
                          type: array
                        enabled:
                          type: boolean
                        identifierJsonPath:
                          description: JSON path expression to extract unique alert
                            identifier used to match triggered alerts with resolving
                            alerts
                          type: string
                        identifierMatchableId:
                          description: The ID of the identifier matchable. If identifier_matchable_type
                            is AlertField, this is the ID of the alert field.
                          type: string
                        identifierMatchableType:
                          description: The type of the identifier matchable. Value
                            must be one of `AlertField`.
                          type: string
                        identifierReferenceKind:
                          description: The kind of the identifier reference. Value
                            must be one of `payload`, `alert_field`.
                          type: string
                        identifierValueRegex:
                          description: Regex group to further specify the part of
                            the string used as a unique identifier
                          type: string
                      type: object
                    type: array
                  secretSecretRef:
                    description: The secret used to authenticate non-email alert sources
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  sourceType:
                    description: The alert source type. Value must be one of `email`,
                      `app_dynamics`, `catchpoint`, `datadog`, `dynatrace`, `alertmanager`,
                      `google_cloud`, `grafana`, `sentry`, `generic_webhook`, `cloud_watch`,
                      `aws_sns`, `checkly`, `azure`, `new_relic`, `splunk`, `chronosphere`,
                      `app_optics`, `bug_snag`, `honeycomb`, `monte_carlo`, `nagios`,
                      `prtg`.
                    type: string
                  sourceableAttributes:
                    description: Provide additional attributes for generic_webhook
                      alerts source
                    items:
                      properties:
                        acceptThreadedEmails:
                          description: Set this to false to reject threaded emails.
                            Value must be one of true or false
                          type: boolean
                        autoResolve:
                          description: Set this to true to auto-resolve alerts based
                            on field_mappings_attributes conditions. Value must be
                            one of true or false
                          type: boolean
                        fieldMappingsAttributes:
                          description: Specify rules to auto resolve alerts
                          items:
                            properties:
                              field:
                                description: Select the field on which the condition
                                  to be evaluated. Value must be one of `external_id`,
                                  `state`, `alert_title`, `alert_external_url`, `notification_target_type`,
                                  `notification_target_id`.
                                type: string
                              jsonPath:
                                description: JSON path expression to extract a specific
                                  value from the alert's payload for evaluation
                                type: string
                            type: object
                          type: array
                        resolveState:
                          description: This value is matched with the value extracted
                            from alerts payload using JSON path in field_mappings_attributes
                          type: string
                      type: object
                    type: array
                  status:
                    description: The status of the alert source. Value must be one
                      of `connected`, `setup_complete`, `setup_incomplete`.
                    type: string
                  webhookEndpoint:
                    description: The webhook URL generated for non-email alert sources
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alertSourceFieldsAttributes:
                    description: 'List of alert fields to be added to the alert source.
                      Note: This attribute requires the alert field feature to be
                      enabled on your account. Contact Rootly customer support if
                      you need assistance with this feature.'
                    items:
                      properties:
                        alertFieldId:
                          description: The ID of the alert field
                          type: string
                        templateBody:
                          description: Liquid expression to extract a specific value
                            from the alert's payload for evaluation
                          type: string
                      type: object
                    type: array
                  alertSourceUrgencyRulesAttributes:
                    description: List of rules that define the conditions under which
                      the alert urgency will be set automatically based on the alert
                      payload
                    items:
                      properties:
                        alertUrgencyId:
                          description: The ID of the alert urgency
                          type: string
                        conditionableId:
                          description: The ID of the conditionable. If conditionable_type
                            is AlertField, this is the ID of the alert field.
                          type: string
                        conditionableType:
                          description: The type of the conditionable. Value must be
                            one of `AlertField or (empty string)`.
                          type: string
                        jsonPath:
                          description: JSON path expression to extract a specific
                            value from the alert's payload for evaluation
                          type: string
                        kind:
                          description: The kind of the conditionable. Value must be
                            one of `payload`, `alert_field`.
                          type: string
                        operator:
                          description: Comparison operator used to evaluate the extracted
                            value against the specified condition. Value must be one
                            of `is`, `is_not`, `contains`, `does_not_contain`.
                          type: string
                        value:
                          description: Value that the extracted payload data is compared
                            to using the specified operator to determine a match
                          type: string
                      type: object
                    type: array
                  alertTemplateAttributes:
                    description: Note that when alert fields are enabled at the team
                      level, alert template attributes cannot be provided. Use `alert_source_fields_attributes`
                      instead.
                    items:
                      properties:
                        description:
                          description: The alert description.
                          type: string
                        externalUrl:
                          description: The alert URL.
                          type: string
                        title:
                          description: The alert title.
                          type: string
                      type: object
                    type: array
                  alertUrgencyId:
                    description: ID for the default alert urgency assigned to this
                      alert source
                    type: string
                  deduplicateAlertsByKey:
                    description: Toggle alert deduplication using deduplication key.
                      If enabled, deduplication_key_kind and deduplication_key_path
                      are required.. Value must be one of true or false
                    type: boolean
                  deduplicationKeyKind:
                    description: Kind of deduplication key.. Value must be one of
                      `payload`.
                    type: string
                  deduplicationKeyPath:
                    description: Path to deduplication key. This is a JSON Path to
                      extract the deduplication key from the request body.
                    type: string
                  deduplicationKeyRegexp:
                    description: Regular expression to extract key from value found
                      at key path.
                    type: string
                  email:
                    description: The email generated for email alert sources
                    type: string
                  name:
                    description: The name of the alert source
                    type: string
                  ownerGroupIds:
                    description: List of team IDs that will own the alert source
                    items:
                      type: string
                    type: array
                  resolutionRuleAttributes:
                    description: Provide additional attributes for email alerts source
                    items:
                      properties:
                        conditionType:
                          description: The type of condition to evaluate to apply
                            auto resolution rule. Value must be one of `all`, `any`.
                          type: string
                        conditionsAttributes:
                          description: List of conditions to evaluate for auto resolution
                          items:
                            properties:
                              conditionableId:
                                description: The ID of the conditionable. If conditionable_type
                                  is AlertField, this is the ID of the alert field.
                                type: string
                              conditionableType:
                                description: The type of the conditionable. Value
                                  must be one of `AlertField or (empty string)`.
                                type: string
                              field:
                                description: JSON path expression to extract a specific
                                  value from the alert's payload for evaluation
                                type: string
                              kind:
                                description: The kind of the conditionable. Value
                                  must be one of `payload`, `alert_field`.
                                type: string
                              operator:
                                description: Comparison operator used to evaluate
                                  the extracted value against the specified condition.
                                  Value must be one of `is`, `is_not`, `contains`,
                                  `does_not_contain`, `starts_with`, `ends_with`.
                                type: string
                              value:
                                description: Value that the extracted payload data
                                  is compared to using the specified operator to determine
                                  a match
                                type: string
                            type: object
                          type: array
                        enabled:
                          type: boolean
                        identifierJsonPath:
                          description: JSON path expression to extract unique alert
                            identifier used to match triggered alerts with resolving
                            alerts
                          type: string
                        identifierMatchableId:
                          description: The ID of the identifier matchable. If identifier_matchable_type
                            is AlertField, this is the ID of the alert field.
                          type: string
                        identifierMatchableType:
                          description: The type of the identifier matchable. Value
                            must be one of `AlertField`.
                          type: string
                        identifierReferenceKind:
                          description: The kind of the identifier reference. Value
                            must be one of `payload`, `alert_field`.
                          type: string
                        identifierValueRegex:
                          description: Regex group to further specify the part of
                            the string used as a unique identifier
                          type: string
                      type: object
                    type: array
                  secretSecretRef:
                    description: The secret used to authenticate non-email alert sources
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  sourceType:
                    description: The alert source type. Value must be one of `email`,
                      `app_dynamics`, `catchpoint`, `datadog`, `dynatrace`, `alertmanager`,
                      `google_cloud`, `grafana`, `sentry`, `generic_webhook`, `cloud_watch`,
                      `aws_sns`, `checkly`, `azure`, `new_relic`, `splunk`, `chronosphere`,
                      `app_optics`, `bug_snag`, `honeycomb`, `monte_carlo`, `nagios`,
                      `prtg`.
                    type: string
                  sourceableAttributes:
                    description: Provide additional attributes for generic_webhook
                      alerts source
                    items:
                      properties:
                        acceptThreadedEmails:
                          description: Set this to false to reject threaded emails.
                            Value must be one of true or false
                          type: boolean
                        autoResolve:
                          description: Set this to true to auto-resolve alerts based
                            on field_mappings_attributes conditions. Value must be
                            one of true or false
                          type: boolean
                        fieldMappingsAttributes:
                          description: Specify rules to auto resolve alerts
                          items:
                            properties:
                              field:
                                description: Select the field on which the condition
                                  to be evaluated. Value must be one of `external_id`,
                                  `state`, `alert_title`, `alert_external_url`, `notification_target_type`,
                                  `notification_target_id`.
                                type: string
                              jsonPath:
                                description: JSON path expression to extract a specific
                                  value from the alert's payload for evaluation
                                type: string
                            type: object
                          type: array
                        resolveState:
                          description: This value is matched with the value extracted
                            from alerts payload using JSON path in field_mappings_attributes
                          type: string
                      type: object
                    type: array
                  status:
                    description: The status of the alert source. Value must be one
                      of `connected`, `setup_complete`, `setup_incomplete`.
                    type: string
                  webhookEndpoint:
                    description: The webhook URL generated for non-email alert sources
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: AlertsSourceStatus defines the observed state of AlertsSource.
            properties:
              atProvider:
                properties:
                  alertSourceFieldsAttributes:
                    description: 'List of alert fields to be added to the alert source.
                      Note: This attribute requires the alert field feature to be
                      enabled on your account. Contact Rootly customer support if
                      you need assistance with this feature.'
                    items:
                      properties:
                        alertFieldId:
                          description: The ID of the alert field
                          type: string
                        templateBody:
                          description: Liquid expression to extract a specific value
                            from the alert's payload for evaluation
                          type: string
                      type: object
                    type: array
                  alertSourceUrgencyRulesAttributes:
                    description: List of rules that define the conditions under which
                      the alert urgency will be set automatically based on the alert
                      payload
                    items:
                      properties:
                        alertUrgencyId:
                          description: The ID of the alert urgency
                          type: string
                        conditionableId:
                          description: The ID of the conditionable. If conditionable_type
                            is AlertField, this is the ID of the alert field.
                          type: string
                        conditionableType:
                          description: The type of the conditionable. Value must be
                            one of `AlertField or (empty string)`.
                          type: string
                        jsonPath:
                          description: JSON path expression to extract a specific
                            value from the alert's payload for evaluation
                          type: string
                        kind:
                          description: The kind of the conditionable. Value must be
                            one of `payload`, `alert_field`.
                          type: string
                        operator:
                          description: Comparison operator used to evaluate the extracted
                            value against the specified condition. Value must be one
                            of `is`, `is_not`, `contains`, `does_not_contain`.
                          type: string
                        value:
                          description: Value that the extracted payload data is compared
                            to using the specified operator to determine a match
                          type: string
                      type: object
                    type: array
                  alertTemplateAttributes:
                    description: Note that when alert fields are enabled at the team
                      level, alert template attributes cannot be provided. Use `alert_source_fields_attributes`
                      instead.
                    items:
                      properties:
                        description:
                          description: The alert description.
                          type: string
                        externalUrl:
                          description: The alert URL.
                          type: string
                        title:
                          description: The alert title.
                          type: string
                      type: object
                    type: array
                  alertUrgencyId:
                    description: ID for the default alert urgency assigned to this
                      alert source
                    type: string
                  deduplicateAlertsByKey:
                    description: Toggle alert deduplication using deduplication key.
                      If enabled, deduplication_key_kind and deduplication_key_path
                      are required.. Value must be one of true or false
                    type: boolean
                  deduplicationKeyKind:
                    description: Kind of deduplication key.. Value must be one of
                      `payload`.
                    type: string
                  deduplicationKeyPath:
                    description: Path to deduplication key. This is a JSON Path to
                      extract the deduplication key from the request body.
                    type: string
                  deduplicationKeyRegexp:
                    description: Regular expression to extract key from value found
                      at key path.
                    type: string
                  email:
                    description: The email generated for email alert sources
                    type: string
                  id:
                    type: string
                  name:
                    description: The name of the alert source
                    type: string
                  ownerGroupIds:
                    description: List of team IDs that will own the alert source
                    items:
                      type: string
                    type: array
                  resolutionRuleAttributes:
                    description: Provide additional attributes for email alerts source
                    items:
                      properties:
                        conditionType:
                          description: The type of condition to evaluate to apply
                            auto resolution rule. Value must be one of `all`, `any`.
                          type: string
                        conditionsAttributes:
                          description: List of conditions to evaluate for auto resolution
                          items:
                            properties:
                              conditionableId:
                                description: The ID of the conditionable. If conditionable_type
                                  is AlertField, this is the ID of the alert field.
                                type: string
                              conditionableType:
                                description: The type of the conditionable. Value
                                  must be one of `AlertField or (empty string)`.
                                type: string
                              field:
                                description: JSON path expression to extract a specific
                                  value from the alert's payload for evaluation
                                type: string
                              kind:
                                description: The kind of the conditionable. Value
                                  must be one of `payload`, `alert_field`.
                                type: string
                              operator:
                                description: Comparison operator used to evaluate
                                  the extracted value against the specified condition.
                                  Value must be one of `is`, `is_not`, `contains`,
                                  `does_not_contain`, `starts_with`, `ends_with`.
                                type: string
                              value:
                                description: Value that the extracted payload data
                                  is compared to using the specified operator to determine
                                  a match
                                type: string
                            type: object
                          type: array
                        enabled:
                          type: boolean
                        identifierJsonPath:
                          description: JSON path expression to extract unique alert
                            identifier used to match triggered alerts with resolving
                            alerts
                          type: string
                        identifierMatchableId:
                          description: The ID of the identifier matchable. If identifier_matchable_type
                            is AlertField, this is the ID of the alert field.
                          type: string
                        identifierMatchableType:
                          description: The type of the identifier matchable. Value
                            must be one of `AlertField`.
                          type: string
                        identifierReferenceKind:
                          description: The kind of the identifier reference. Value
                            must be one of `payload`, `alert_field`.
                          type: string
                        identifierValueRegex:
                          description: Regex group to further specify the part of
                            the string used as a unique identifier
                          type: string
                      type: object
                    type: array
                  sourceType:
                    description: The alert source type. Value must be one of `email`,
                      `app_dynamics`, `catchpoint`, `datadog`, `dynatrace`, `alertmanager`,
                      `google_cloud`, `grafana`, `sentry`, `generic_webhook`, `cloud_watch`,
                      `aws_sns`, `checkly`, `azure`, `new_relic`, `splunk`, `chronosphere`,
                      `app_optics`, `bug_snag`, `honeycomb`, `monte_carlo`, `nagios`,
                      `prtg`.
                    type: string
                  sourceableAttributes:
                    description: Provide additional attributes for generic_webhook
                      alerts source
                    items:
                      properties:
                        acceptThreadedEmails:
                          description: Set this to false to reject threaded emails.
                            Value must be one of true or false
                          type: boolean
                        autoResolve:
                          description: Set this to true to auto-resolve alerts based
                            on field_mappings_attributes conditions. Value must be
                            one of true or false
                          type: boolean
                        fieldMappingsAttributes:
                          description: Specify rules to auto resolve alerts
                          items:
                            properties:
                              field:
                                description: Select the field on which the condition
                                  to be evaluated. Value must be one of `external_id`,
                                  `state`, `alert_title`, `alert_external_url`, `notification_target_type`,
                                  `notification_target_id`.
                                type: string
                              jsonPath:
                                description: JSON path expression to extract a specific
                                  value from the alert's payload for evaluation
                                type: string
                            type: object
                          type: array
                        resolveState:
                          description: This value is matched with the value extracted
                            from alerts payload using JSON path in field_mappings_attributes
                          type: string
                      type: object
                    type: array
                  status:
                    description: The status of the alert source. Value must be one
                      of `connected`, `setup_complete`, `setup_incomplete`.
                    type: string
                  webhookEndpoint:
                    description: The webhook URL generated for non-email alert sources
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
